// config.js — commit this file, but never put your token here.
// Instead, the app reads the token from localStorage key "gh_token"
// so you only enter it once in the browser.

const GITHUB_CONFIG = {
  owner: "dirtypaws",   // e.g. "mattsmith"
  repo:  "workout-tracker",               // the repo name
  branch: "main",
  dataFile: "data.json"
};
